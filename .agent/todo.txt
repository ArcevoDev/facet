facet - session plan (continuation)
Status: 2nd commit staged (playground fixes) - ErrorBoundary + QrScanner
+ AnnouncementBar/PasswordStrengthMeter demo wrappers. Push pending user
confirmation.
All 16 priority items complete. Working tree clean (only debug
trace-output.txt + scratchpad/ remain as session artifacts).
PRIORITY LIST
[x] 1. KANBAN card actions - AlertDialog for Delete (replaced window.confirm)
[x] 2. CHART hover - fixed hardcoded seriesIndex: 0
[x] 3. NAVBAR hover dropdown blink - DropdownMenu fights hover timer (z-index 50 -> 60 done)
[x] 4. DOCS z-index - mobile sidebar z-[40] -> z-[70] (above topbar/navbar z-60 so it overlays on small screens); topbar z-20 -> z-60; navbar z-50 -> z-60
[x] 5. PLAYGROUND FORMAT - Prettier auto-indent formatting on the editor (Format button, lazy prettier/standalone) [DONE - pre-existing in working tree; tests pass]
[x] 6. PLAYGROUND COMPONENTS NOT DISPLAYING - shared registry (facet-components + auth + layout + placeholders) + parseLiteral returns undefined for bare identifiers [DONE - added 26 USAGE entries for missing components + StepperProvider/KanbanBoard demo wrappers]
[x] 7. PLAYGROUND CONSOLIDATION - ComponentPage playground shares LiveCodePlayground + playgroundComponents registry with /playground [DONE - pre-existing in working tree]
[x] 8. BorderBeamCard animation / hover
[x] 9. DOCS-SITE changelog wiring
[x] 10. EM DASH PURGE
[x] 11. LIVE PLAYGROUND - copy button, full code, bare-JSX fallback, max-height
[x] 12. CENTRALIZE /playground - ecosystem sidebar + dynamic selector
[x] 13. PLAYGROUND TESTS - LiveCodePlayground.test.tsx
[x] 14. PLAYGROUND ErrorBoundary - component render crashes now surface a
    message instead of hanging the Suspense PageLoader ("lazy load")
[x] 15. PLAYGROUND QrScanner - autoStart={false} so preview doesn't request
    camera permission
[x] 16. PLAYGROUND AnnouncementBar + PasswordStrengthMeter - demo wrappers
    (unique storageKey / fallback value) so previews always render
NOTES
- z-index scale (IN SYNC across repo):
    z-30 = persistent desktop sidebar (in-flow)
    z-[50] = (retired; Radix portaled content now lives at z-[70])
    z-60 = Application chrome (Topbar, Navbar) - sticky headers/overlays
    z-[70] = Radix portaled overlays (Dialog, Drawer, Sheet, AlertDialog)
              + Navbar/UserAvatar DropdownMenuContent (above z-60 chrome)
    z-[80] = Mobile sidebar overlay - MUST sit above z-[70] overlays so
              it covers them on small screens
- Build gate: pnpm -r build / typecheck / test / check:docs / check:icons / check:sdk-drift
- Two changesets present (kanban-actions + playground-robustness).
  CI will auto-open a Version Packages PR on main once pushed.
