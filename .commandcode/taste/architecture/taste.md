# architecture

- Prefers clean directory scaffolding: empty stub directories that serve no purpose should be flagged and either populated or removed; every directory should have intentional content. Confidence: 0.85
- Prefers domain-namespaced subdirectory organization (e.g., `forms/auth/`, `forms/mfa/`) with barrel exports resolving upward to a root barrel, keeping related files grouped by domain rather than flat. Confidence: 0.8
- Uses barrel exports (index.ts re-exports) for grouped directories to improve import readability and provide clean public API surfaces. Confidence: 0.85
- Prefers splitting large files (200-300+ lines) into smaller, focused modules extracted by concern, for future-proof maintainability and independent testability. Confidence: 0.85
- Auth components must be domain-customizable (different configs for fintech vs med vs edu): not one-size-fits-all. Confidence: 0.85
- Include a welcoming landing page and deployed documentation site alongside the component library. Confidence: 0.70
- Landing page must have real routed pages with actual content (components gallery with live demos, docs/design-tokens, theming, changelog) instead of dummy/placeholder sections and fake nav links: stacked one-page sections with routes that don't exist are flagged as a gap, not acceptable for a public site. The landing should also carry a roadmap section and other consumer-beneficial content the assistant deems useful ("do not forget our roadmap section we wanted to enhance our landing with and other beneficials you think a consumer needs displayed"), with routing reshaped as needed. Confidence: 0.65
- Interested in modern micro-interaction and visual polish (gradient text, typing animation, smooth scrolling, hover/micro-interactions) as UI enhancements for the landing/marketing surface: welcomed as post-release polish items, not release blockers. Confidence: 0.6
- Prioritize component development by developer-time-saved: build what takes devs the most effort to implement (navbar variants, sidebars, notification drawers, etc.) over visually flashy additions. Value-to-effort ratio is the primary selection criterion for new components. Confidence: 0.8
- Ship functional components first, defer animations and visual polish until after the first public release: "beautifying" is a post-launch activity, not a pre-launch blocker. Animation and visual decoration should not delay the initial public package. The user explicitly defers icon hover micro-animations (e.g., a trash icon whose lid opens/closes on hover) to a later version, gated on the library being stable, cleanly consumed, and truly agnostic. Confidence: 0.9
- Prefers a custom-built component showcase/docs site (with live previews, props tables, code copy, search) as the public documentation surface over exposing raw Storybook: Storybook remains an internal dev tool. Modern UI systems (shadcn/ui, Chakra, Radix) are the reference for how the public site should look and feel. Confidence: 0.65
- Prefers lucide-react icons over emoji characters on landing pages and marketing surfaces: emoji in feature cards or navigation looks unprofessional on a public-facing site; lucide provides consistent, scalable vector icons. Confidence: 0.8
- Landing page must present as a general-purpose open-source component library (like shadcn/ui, Radix, PrimeReact): not as an internal ecosystem tool. Copy, badges, and messaging should appeal to external developers and describe general value props, not internal repo consolidation or org-specific tooling. Confidence: 0.8
- Dogfood the library's own components on the landing/marketing page to validate real-world rendering and prove components work in a real app context: the landing page is a first-hand testing surface, not just a marketing page. Confidence: 0.8
- Provide a sandbox, playground, or interactive sample space (within Storybook or alongside it) where all components can be explored, demoed, and tested: not just documented in isolation. Confidence: 0.75
- Build a differentiated component library wired to arc-id SDK, not a generic clone of existing UI libraries. Confidence: 0.65
- Prefers npm package installation as the primary consumption model for UI libraries (install and import directly like react-email), with CLI component init (like shadcn add) deferred as a secondary tier. Confidence: 0.8
- Prefers publishing packages as public on npm (not private/internal), using changesets for versioning/changelogs and a CI/CD pipeline with GitHub Actions. Confidence: 0.8
- Uses Storybook as the development preview environment for testing and validating components before deployment. Confidence: 0.8
- TypeScript type declarations (e.g., `vite-env.d.ts` with `/// <reference types="vite/client" />`) must exist in EVERY directory that contains files importing non-TS assets (CSS, images, etc.), not just `src/`: Storybook config files in `.storybook/` import CSS and must have their own declaration file to avoid IDE type errors. Confidence: 0.7
- Uses MDX documentation pages (getting-started, contributing, design-tokens, theming, auth-guide) alongside component stories in Storybook, and expects them to compile in production builds: not just in dev server mode. Requires explicit `@storybook/addon-docs` configuration with Vite 8 compatibility adjustments for production. Confidence: 0.75
- Keeps `tsconfig.json` project references synchronized with actual directory structure: stale references to nonexistent directories must be removed to prevent build toolchain (rolldown resolver) failures. Confidence: 0.65
- When fixing a reported issue, perform a comprehensive cross-repo gap analysis: audit ALL sibling repos (arc-id, arcbase, arc-wallet) to identify and close every related gap, not just the immediately reported issue. Single-issue fixes are incomplete; a full gap matrix across all consuming repos is expected, and gaps identified in docs must be closed once and for all. The reverse direction holds too: when the user references a fix already proven in a sibling repo (e.g., stream-wise's vercel.json SPA-fallback rewrite for the Vercel deep-route refresh-404), the same proven fix is applied to the current repo's equivalent surfaces (both apps/docs and apps/landing lacked vercel.json, so both got the fallback + cache/security headers) without re-deriving it. Confidence: 0.9
- Storybook is the verification surface for visual fidelity: components aren't "done" just because they typecheck; they must also render with proper styling (colors, spacing, layout) in the dev preview. Confidence: 0.85
- Prefers scalable, generalizable implementations over one-off builds: approval for follow-through work is conditioned on the approach being scalable ("if scalable... let's get the demo work and every other necessary 3rd party ready configurations done"), and the accepted solution turns a bespoke demo into a reusable, config-driven primitive (a first-class `authDemo` docs block type consumers can drop into any page, exported from the package barrel) rather than a single hardcoded page edit. Re-confirmed when the user chose package-level code-splitting (a new `@arcevo/facet-components/light` subpath + lazy docs engine) over app-local bundler patches so every consumer of the packages benefits. Confidence: 0.7
- When given honest tradeoff options about how deep to take a fix (e.g., bundle/performance work), the user consistently chooses the deeper, correct option that delivers the real win: accepting a package API change (a new `/light` subpath, deep imports) and more scope over shipping a partial improvement or reverting. At three consecutive decision points the user picked the "deep fix" option once the assistant measured and explained that the shallow version (route-level lazy loading alone) did not actually cut initial load — so the assistant should present the measured tradeoff and recommend the complete fix rather than settling for marginal gains. Confidence: 0.65
- Prefers a framework-agnostic, composable layout shell (slot-based: <Shell sidebar topbar>content</Shell>) that consumers wire their own routing into: not a framework-coupled layout. Confidence: 0.7
- Prefers using the latest stable major versions of core dependencies and tools (e.g., Tailwind CSS v4 with Rust parser) rather than falling behind on major releases. Confidence: 0.7
- Prefers centralized shared configuration and design tokens at the monorepo-root or shared-package level, not duplicated per app: each app should only hold per-entrypoint wiring (e.g., Vite plugin instantiation), not shared config. Confidence: 0.65
- Design packages for unknown 3rd-party consumption: API surfaces, configurability, and docs must serve developers who have no knowledge of internal arc-ecosystem conventions; not just internal reuse across company repos. Confidence: 0.85
- A product monolith (frontend+backend in one package) is acceptable as the product shape as long as the reusable/consumable surfaces (CLI, SDKs, UI packages) are separate published packages and the internal seams stay clean (API routes never import app internals, the SDK never imports the API server, the app talks to the API only via the SDK + fetch) so a later extraction is mechanical, not a rewrite: the user asked whether the single frontend+backend package is OK "although, we would have the cli and sdks as pkgs rii," endorsing that structure. Confidence: 0.55
- Wants the ecosystem's cross-language/framework agnosticism assessed explicitly, not assumed: for the components package, asks "how agnostic is our system overall.. can a py dev integrate in their project... or it is locked on js-ts devs" — the answer must state the honest boundary (React-only components by design; the SDK/CLI are the language-agnostic surfaces a Python dev can consume via a React frontend) rather than passing a JS/TS-only scope silently. Confidence: 0.55
- Before designing a shared abstraction package (e.g., layout), audit all consuming repos (arc-id, arcbase, arc-wallet) to find true shared patterns and avoid premature abstraction. Confidence: 0.7
- Rejects `as unknown as never` and similar type-coercion hacks: type safety is non-negotiable; dead type escapes must be removed in favor of honest optional typing. Confidence: 0.9
- Shared packages within the monorepo must consume other sibling shared packages (e.g., auth package uses the components package's Button, Dialog, etc.) rather than duplicating UI patterns or rolling custom raw elements: diversion from this dependency chain is flagged as a problem. Confidence: 0.7
- The component library should be icon-library-agnostic: icons must be accepted as props or slots, not hardcoded, so consumers can wire in their icon library of choice (lucide, react-icons, etc.) without being forced into a vendor lock-in. The library should not ship its own icon set. Confidence: 0.7
- Domain presets/templates should be icon-library-agnostic: they express domain structure (href, label, badges), not visual wiring. Consumers bring their own icon library; presets don't import or fake icon dependencies. Confidence: 0.85
- README is a primary documentation surface, not a placeholder: must include a full packages table with status, quick-start code example, architecture explanation, and consumption model. Confidence: 0.85
- Uses Tailwind CSS v4 with a shadcn-style theme (CSS custom properties mapped to utility classes like `bg-background`, `text-foreground`) as the styling approach for the UI component library. Confidence: 0.9
- Typography must be explicitly configured in the Tailwind v4 `@theme` block: `--font-sans` must map to the project's actual body font (e.g., Inter via `--font-body`), and `--font-mono` to the code font (e.g., JetBrains Mono); relying on Tailwind v4's system-ui defaults results in visually unacceptable rendering that the user flags as "ugly." Confidence: 0.85
- Visual quality is verified by inspecting the rendered output in a browser, not by build success alone: the user calls out when "everything looks the same" (components lack styled differentiation) or fonts appear ugly; checking that the CSS toolchain ran is insufficient without confirming the actual visual result meets a polish bar. Confidence: 0.85
- Styling pipeline (Tailwind config + PostCSS + stylesheet with @tailwind directives) must be fully wired before components can be considered functional: bare utility classes without a working CSS toolchain are invisible styling debt. Confidence: 0.85
- When using Tailwind v4 in a pnpm workspace monorepo, the Rust-based scanner does not automatically crawl compiled workspace dependency sources for class names; explicit `@source` directives pointing to each workspace package's `src/` directory are required in the consuming app's entry CSS so utility classes used in shared component packages are generated in the final bundle. Confidence: 0.8
- Storybook stories must be wired and registered so components display in the preview; components that exist in code but don't show up in Storybook are flagged as an unresolved gap, not silently considered done. Confidence: 0.75
- Components should support icon-flexible variants: e.g., a badge that works with an optional icon, including an icon-only badge; icons optional by default rather than required. Confidence: 0.65
- The library should ship theme support: a theme toggle component and runtime-switchable themes customizable per consumer preference, exposed on the landing page so layouts can switch themes. Confidence: 0.7
- All components must have their tests wired as part of completion: unwired test coverage across the component set is a flagged gap, not an acceptable state. Confidence: 0.6
- Component tests against Radix primitives (Select, DropdownMenu) in this repo's jsdom setup must account for portal rendering and pointer-capture quirks: select listboxes portal to `document.body`, so options must be found with `screen.findByText` after opening (not the default render container); synthetic `userEvent.click` on a trigger does not reliably open the menu, so open via `fireEvent.keyDown` (ArrowDown) on a focused trigger or `fireEvent.click` on the trigger; and controlled inputs that fire `onValueChange` per keystroke should be asserted with a single `fireEvent.change` rather than `userEvent.type` (which asserts only the last character). Confidence: 0.7
- Prefers the modern "pill" navbar aesthetic used by current software: a floating rounded bar with frosted-glass backdrop blur (content scrolling behind it appears blurred/translucent), with a segmented link tray and active chip : over classic full-width sticky bars, including on marketing/landing pages. Confidence: 0.7
- Layout shells should ship as two responsive variants: (1) a full sidebar with labels, and (2) a sidebar collapsible to an icon-only rail with tooltips : both must be screen responsive (e.g., collapsing to a mobile sheet), with the collapsed choice persisted. Confidence: 0.8
- The collapsible sidebar should follow the VS Code IDE interaction model ("adjustable collapsible"): a drag handle on the right edge resizes the expanded width, dragging below the minimum width auto-collapses it to the icon rail, and clicking any rail icon (or a keyboard shortcut like ctrl+b) expands it again. The user explicitly requested this behavior for the docs-site sidebar over a plain hamburger toggle. Confidence: 0.6
- Prefers the expanded/full sidebar to be text-only (labels, no icons), with icons appearing only in the collapsed icon-rail state as per-section landmarks: icons are for the collapsed rail, not decoration in the full menu. Confidence: 0.65
- Component and layout names must unambiguously reveal their purpose: a generic name for a specialized shell (e.g., AppLayout that is really an auth page shell) is misleading and should be renamed to something explicit (e.g., AuthLayout), keeping a deprecated alias for backwards compatibility. Confidence: 0.75
- All components : auth included : must be dynamically customizable by consumers through prop families: `appearance` (className overrides), `slots` (content replacement), and `config` (Partial config/behavior overrides). Presets should be plain objects consumers can spread and extend per-instance. Confidence: 0.8
- Navbar variants must be full-featured, not reduced: the pill variant must support actions, badges, and sub-links (dropdowns) exactly like other variants; a variant is not a stripped-down version. Confidence: 0.6
- Prioritizes speed to the initial npm publish: check tracker validity, resolve publish-blocking issues, and publish ASAP rather than accumulating more features, variants, or polish before the first release. The user has downstream projects lined up to consume the packages ("i have other projects to consume our ready packages... let us deploy") and wants publish/deploy to proceed the moment the package is consumable. Confidence: 0.65
- Asks for alternative versions/shapes of a design or delivery model before committing ("what other version of this could be"): e.g., fork-and-customize source template vs config-only theme package vs multi-tenant hosted docs : the assistant should lay out the options with their tradeoffs rather than presenting a single design as final. Confidence: 0.55
- Proactively removes deprecated API usage: deprecated aliases (e.g., React.ElementRef → React.ComponentRef, z.string().email() → z.email()) must be replaced with the modern non-deprecated form, following the pattern already standardized across the rest of the codebase (confirmed from other components) rather than inventing a new one. Confidence: 0.8
- Requests an explicit codebase-solidity assessment for both internal and external use before release: expects a go/no-go verdict that calls out concrete blockers and known gaps (e.g., npm auth, uncommitted changes, broken lint), not just a green build/test run. Repeatedly asks "is this ready to be integrated in our internal systems like ../arc-id, and external system like ../stream-wise" : the readiness answer must cover each named consumer's actual state (whether it even consumes the package, what it pins, whether it's installed). Confidence: 0.85
- Release/integration readiness is gated on git and publish state, not just code correctness: a package change set is only consumable by downstream systems (internal like arc-id, external like stream-wise) once committed, pushed, and : if the change touches the published artifact : version-bumped and republished. The user explicitly asks whether uncommitted/unpushed changes require a republish before downstream integration, so the assistant should state the release impact (is a bump/republish actually needed for this change set?) as part of the readiness answer. Confidence: 0.6
- Config files should be kept minimal and conservative: speculative workaround plugins/filters in build configs (e.g., unshifting a `react()` babel transform, extra `vite:*` filters added to "fix" a dev-mode hang) are reverted in favor of the known-good committed config; only harmless improvements (e.g., `chunkSizeWarningLimit`) are kept. Experimental config churn that doesn't fix the problem adds risk to a working build. Confidence: 0.65
- Version numbers must signal finished product status, not pre-release semantics: anything release-ready (including private apps like the landing page) should be at `1.0.0` : a `0.x` version "makes it read api or unfinished," so release-ready packages/apps get bumped to 1.0.0 even when the bump is cosmetic (e.g., private apps never published to npm). Confidence: 0.6
- Navbars must be responsive-first on small screens: all actions and CTA buttons (e.g., GitHub link, "Browse components" button) should collapse into the hamburger dropdown so they don't "compact too much the space" in the top bar; only essential always-reachable controls (e.g., theme toggle) stay in the top bar at all sizes. Shared navbar components should support this (custom mobileMenu slot) and auto-close the menu on item click. Confidence: 0.7
- When expanding the component library, benchmark against popular UI libraries (e.g., shadcn/ui's component set) to identify missing essentials and decide what to build: the user asks to "look into some popular ui libraries and check the various [components] we can build and the ones we can create for dynamism" before committing to a new batch : popular-library parity acts as the gap checklist, not just internal needs : the user repeatedly asks whether the current component set is enough or more should be built ("do you think we have enough for now or we can have more") before committing to expansion. Confidence: 0.75
- Prefers a lean dependency footprint when adding components: build composed components from already-installed primitives where possible, and add only small focused deps (e.g., the missing @radix-ui primitives) while deferring heavy libraries (recharts, embla, react-day-picker, tanstack-table, vaul) to later batches. Confidence: 0.6
- The docs-site must be a generic, installable, fully-customizable artifact, not a project-specific site with baked-in branding: branding (site name, logo, tagline, colors/fonts via theme overrides) and content (nav tree, pages, footer) should all be config-driven (a single siteConfig), so the same docs site can be installed into any other project and restyled without editing source pages : the goal is "install and fully customize," including hosting the library's own production docs on it (explicitly contrasted with Storybook, whose branding is default and not customizable). The user explicitly expects the doc-site itself to be published as a package: ship it as a demo/template that consumers install and customize with their own stories and sidebar links, rather than a site only the library itself can host. Confidence: 0.85
- The docs-site sidebar must stay a clean, text-only link list : no inline previews inside sidebar items and no per-item "View all variants" links: inline previews in the sidebar look ugly and clutter the navigation. This explicitly reversed an earlier "Storybook-browsing-model sidebar" direction once the user saw it rendered; sidebar links simply navigate to the component's page. Confidence: 0.9
- The components index page (the list of all components) must use clean pagination : 10–20 components per page : never dumping the full component set (e.g., 50) on one screen at once. Confidence: 0.8
- The components index grid must be responsive with breakpoint-aware column counts and no tiny multi-column layouts: 1 column on small screens, at most 3 on medium, and 4–5 on large/desktop; the manual column control offers only 3/4/5 (1- and 2-column options are not allowed) and only takes effect on large screens. Confidence: 0.7
- The components index should have its own local search box (scoped to the gallery, distinct from the global command-palette search) that filters by name/slug/category with a no-results empty state and clear action. Confidence: 0.65
- Component listing cards default to showing a live preview and are collapsible down to just the component name, so the index stays light and scannable. Confidence: 0.7
- Clicking a component in the list routes to a dedicated per-component page (e.g., /components/:slug) where the previews and all variants are shown; the preview belongs on that page and the index, not in the sidebar. Confidence: 0.7
- Include the common "set up your X in N steps" onboarding guide pattern on the docs site and landing page: users landing on a site/app expect a step-by-step getting-started flow (step 1: do this / set up your X in N steps), and the docs should provide it as a real pattern (e.g., a "Setup in 3 steps" quickstart on the landing page and step-by-step guide in the docs) using existing step/accordion/card components. Confidence: 0.65
- Docs pages must be as dynamic as the nav: routes, sidebar sections, and search should all derive from a single pages registry (a page is data : path, title, section, content blocks), so adding a page to the registry automatically adds its route and nav entry with zero component edits : the user asked for "a mini internal way to ensure that the pages are as dynamic as the nav." Confidence: 0.75
- Component usage sections should present per-variant usage code as tabs: one tab per variant label (matching the variant gallery), each showing that variant's code with a copy button, falling back to a single Default tab for components without a variant gallery. Confidence: 0.8
- Prefers a high-contrast "frost" surface over heavily translucent "glass" wherever readable body text must sit on top: a glassy alert dialog with washed-out, low-visibility text was flagged as a defect and replaced with a more opaque frosted surface (higher-opacity background, lighter border, subtle inset highlight) : "glass" remains acceptable for decorative/hero areas, but any surface carrying body text gets the more opaque frost treatment. Confidence: 0.7
- Dialog/overlay surfaces must stay consistent with the Card component's surface treatment across ALL dialogs (AlertDialog, Dialog, compact/blurred variants): the user asked to "ensure that the card components used across all dialog is perfect," so dialog content should reuse the card-style frosted surface by default rather than plain `bg-background` or one-off styling per dialog type. Confidence: 0.65
- Visual surface effects like glass/frost must be customizable via CSS variables (background opacity, blur radius, border, shadow) defined in the design tokens, with per-theme (dark/light) defaults, so consumers can tune the frost/glass to their own themes : the user explicitly asked "is there a way to customize the frost." Confidence: 0.7
- Sidebar nav sections should be optional and config-driven rather than hardcoded: a special section pinned as the last sidebar item (e.g., an "Ecosystem" section) should be customizable so consumers can add/remove links to their future ecosystem docs there for easy discovery and routing. Confidence: 0.55
- Docs install blocks should use tabs for different package managers (pnpm, npm, yarn, bun), like other documentation sites, so users can run the right command for their setup: the framework-agnostic consumption story means install instructions are presented per-manager, not as a single hardcoded command. Confidence: 0.8
- Only display install commands that are actually valid for the package: do not write a command for a package manager that can't install the package : each manager's compatibility with the package must be verified before showing its tab. Confidence: 0.7
- Package-manager install tabs should be compact in size: wide tab controls on large screens "stress the consumer," so installer tabs must stay visually tight, not stretch to full width. Only the manager chips should be compacted : the install command/code block itself stays full-width (the user corrected an attempt to cap the whole block's width). Confidence: 0.7
- Layout shells (AuthLayout, LandingLayout, ConsoleLayout) must be fully composable: consumers should be able to replace the entire decorative/brand panel with arbitrary content — a ReactNode slot prop (e.g., `brandPanel`) that fully replaces the default config-driven panel (logo/tagline/benefits), plus a className override for the panel wrapper (e.g., `brandPanelClassName` for custom bg/gradient/image) — so anything (image slideshow, video, Lottie, custom hero) can be dropped in, with the config-driven default preserved when the slot is omitted. The user asked whether the auth layout can host "img slideshows.. or lottie displays.. or video or anything at all," and the fix was a backward-compatible slot rather than a fork. Confidence: 0.6
- A new public prop/API on a package is not "done" until the built package is consumed: docs/consumer apps resolve sibling packages through their built `dist` (npm resolution), so a newly added prop stays a type error in consumers until the package is rebuilt — e.g., the docs typecheck failed on `brandPanel` until the layout package was rebuilt, then passed. Verify consumer typechecks AFTER rebuilding the dependency package, not before. Confidence: 0.6
- Every new public API/prop should ship with a matching test, a usage snippet, and a live variant preview cell kept in lockstep (a new `brandPanel` prop got a replacement test, a video-example usage snippet, and a "Custom brand panel" variant tab + preview cell): docs examples and tests are part of the API's definition, not optional extras. Confidence: 0.55
- The components index/grid must preview exactly ONE default (representative) variant per component : never all variants: an accordion that rendered all five of its variants inside the general components page grid was reported as a defect ("its supposed to preview only one"); the full variant gallery belongs on the dedicated per-component page and stories, not in the index grid. Confidence: 0.8
- Data-heavy component toolbars should consolidate scattered actions into icon-plus-menu chrome: one icon Export button that opens a format dropdown (built-in CSV via `exportable` + pluggable custom `exporters[]`), plus an ellipsis (⋯) overflow menu for pluggable bulk row actions (select all, mark as read, delete all, custom) where each action receives both the full rows and the currently selected rows, instead of a row of separate labeled buttons. The user requested this exact toolbar shape for DataTable ("the export button with icon, and maybe some ellipses icon to choose customized table actions") and uses the export menu as the place to test exports. Confidence: 0.65
- A new/refined component is not "done for docs" until it has a LIVE preview on the docs site: manifest entries, variant cells, and usage snippets are insufficient — the preview switch must have a case for the slug and a demo component behind it (DateInput, PasswordInput, and InfiniteScroll had all the docs wiring yet still rendered "live preview ... not implemented yet" until demos were added and wired into previews.tsx). The user asks "are we sure that all the newly refined or rebuilt components are displaying in the docs-site for proper preview" and then manually runs through them, so every shipped component must be verifiable as a rendered live demo, not just present in code. Re-affirmed when the QRCode `logo` variant — already implemented and unit-tested — was still flagged until a live demo with a real brand mark and a position switcher proved it renders on the docs site. Confidence: 0.7
- Docs-site keyboard shortcuts should be documented in a single discoverable place : the Getting Started page (chosen over the Settings menu or the Overview page when asked) : with platform-aware modifier labels rendered via the Kbd component (Ctrl on Windows/Linux, ⌘ on macOS). Confidence: 0.75
- Docs-site keyboard navigation must use plain Alt+arrow chords only: Ctrl/Cmd+Alt+arrow combinations collide with a Windows OS-level shortcut that flips screen orientation ("that does something else entirely on windows"), so they were removed and reverted to plain Alt+←/→ for paging the components list and Alt+↑/↓ for prev/next component on per-component pages; shortcuts are ignored while typing in form fields. Confidence: 0.75
- The custom docs-site app is the authoritative docs surface over the legacy Storybook app: when the story sets in `/docs` (facet-docs) and `/docs-site` (facet-story) drift or overlap, discrepancies must be resolved in favor of `/docs-site` : the user explicitly set this priority ("if any has to be higher... it should be /docs-site"). The user views the old Storybook docs as replaceable by a publishable docs package built for external reusability ("keep the old storybook docs or build a docs pkg with the necessary details to publish for reusability externally"), and expects the assistant to verify the two story dirs are actually in sync before giving a recommendation. Confidence: 0.7
- Rejects source-fork/copy as the customization model for published docs packages: consumers should get flexibility via install-and-configure (a config + routes-driven shell API : brand, nav sections, ecosystem links, and their own pages passed in as routes) rather than having to copy the shipped `src/` or fork the repo. The user explicitly asked "is there a way to make it flexible without copying the src folder or forking the repo" after the fork-and-customize template model was presented. Confidence: 0.7
- The landing page must be kept in sync with the current library state: stale stats (e.g., "35+ components" when the actual count is 47), outdated package lineups, and dead links to old docs URLs are unacceptable and prompt a full rewrite : the user explicitly stated "we need to fully rewrite our landing page to reflect our recent state." Every public claim must be verified against the actual package/API before it ships: the assistant greps real exports to confirm stat counts (10 SDKs, 5 auth presets, 3 layout shells — not 5), checks the actual IconProvider/registerIcon API before writing the icon-override FAQ answer, replaces the publisher-only "changeset publish" step with a consumer command, keeps the public roadmap phases in sync with the internal .agent tracker, and in a later batch fixed stale docs claims (52 → 57 components, added facet-cli to the package list, corrected the publishing note to "CI is validation-only, publish is local", and replaced the nonexistent `fintechAuthPreset` snippet export with the real `fintechPreset`). Confidence: 0.95
- The icon registry in the components package (semantic names wrapping lucide-react) is expected to be the single icon surface for the monorepo: direct `lucide-react` imports in sibling packages (docs, layout) and apps are flagged as drift ("why are we using lucide again"), and icon usage should flow through the registry instead. Confidence: 0.7
- Prefers a single dynamic `<Icon name="lowercase-name" className size />` component as the consumer-facing icon API over per-icon named imports: consumers should reference icons by lowercase name through one Icon component (className and size optional props), and the raw icon export file (e.g., `icons.ts` re-exporting lucide-react) is only a low-level escape hatch, not something consumers need to know about. Confidence: 0.6
- Brand/social icons that lucide-react has deprecated should be remade in-house the "Github way" and owned by the icon registry: inline SVGs matching lucide's stroke style (viewBox 24, stroke=currentColor, strokeWidth 2) registered in the registry's default icons so they win name resolution, with the icon-map generator excluding deprecated lucide brands — so `<Icon name="whatsapp" />` renders a working icon instead of null/deprecated components and the registry becomes the single home for brand icons. Confidence: 0.6
- When the registry is missing icons that consumer code needs, the default response is to extend the registry with the missing icons ("if the registry does not have all the icons.. can we just update with all") rather than adding more direct lucide imports in consuming code. Confidence: 0.65
- When code-splitting with `React.lazy` in React Router v7, the Suspense boundary must wrap the entire `<Routes>` (or the router consumer), not individual `<Route element>`s: a lazy layout route that renders `<Outlet />` with child routes, given only a per-route Suspense, silently stalls the whole tree — blank page, `#root` empty while React is still attached, the lazy chunk never fetched, no console error — and the fix is one `<Suspense>` around `<Routes>`. Verified in a real browser against the production build (home, /components gallery, and /components/:slug detail all render after the fix). Confidence: 0.85
- Consumer-facing validation of the facet icon registry must reference canonical lucide v1.30 kebab-case names (e.g., circle-alert, loader-circle, chart-column, circle-x, shield-check, face-slightly-smiling) and never deprecated lucide aliases (e.g., history, smile): deprecated aliases resolve to different glyphs than the canonical icon, so validating against them yields false results. Confidence: 0.7
- Data-heavy components that ship reference datasets (country codes, regions, locales) should default to COMPLETE/inclusive data (e.g., all ISO country codes) with consumer-set restrictions as an opt-in variant (include/exclude/regions props), not a small hardcoded subset. Re-affirmed for the LocationPicker when the user directed "you can just use the world map for what exiss" — populate the full real-world dataset rather than a curated sample. Re-affirmed again when the user directed full official first-level state/region lists for EVERY covered country once Nigeria had the full set ("i think we should have the dynamic full state list across other countries too, at least..") — completeness must be uniform across all covered countries, not just a flagship one (Nigeria full while US/India had 4-5 was flagged as lopsided). Confidence: 0.75
- Static reference datasets bundled with components (countries, states/regions, localities) should be externalized into dedicated data module files (e.g., location-data.ts) that the component imports and re-exports, rather than inlined in the component: the user proposed "put all the countries and states in a file to resolve them dynamicallly into the component" so the cascade (country → state → LGA) resolves from an importable/overridable dataset, with the barrel API unchanged (DEFAULT_COUNTRIES / DEFAULT_REGIONS / DEFAULT_LOCALITIES still re-exported). Confidence: 0.55
- Cascading/dependent selects should render a resolved parent tag on their trigger (a small uppercase country-code badge plus the resolved country name) so each level shows which parent entity its options belong to, updating dynamically when the parent changes: requested explicitly for the LocationPicker's State and LGA selects ("the state would dynamically have like a country tag, tha resolves based on the chosen country, same for the lga"). Confidence: 0.5
- Prefers API-driven data for data-heavy components when an API would make them better ("if there is an api to make these better... we could better utilize"), over bundled static datasets. Confidence: 0.5
- Library roadmap targets locale-specific input types with Africa-first coverage (starting with Nigeria: country, state/province, LGA/LCDA inputs) alongside generic ones, since administrative divisions differ per country. Confidence: 0.5
- Consumers should customize the library's look through the tokens package (theme variables, ThemeProvider + overrideVars) rather than restyling many components individually: the tokens path is the "save them the stress of restyling many components" story and the recommended customization route for consumers. Confidence: 0.7
- Dislikes bordered/outlined button treatment for nav-level actions on the landing page: a nav action like "Browse components" should not be an icon inside a bordered Button — the "border feel" is explicitly disliked; the action should stand borderless like the theme toggle (icon-only, hover-bg, no border), applied consistently to desktop and mobile menu entries. Confidence: 0.7
- When a dependency deprecates APIs (e.g., lucide-react deprecating brand/social icons like Github, Linkedin, Instagram, Facebook), purge them repo-wide ("purge the remaining [icons] anywhere"), not just in the reported file: replace deprecated imports with non-deprecated alternatives such as inline SVG brand icons that preserve the underlying functionality (e.g., social links keep working), and verify zero deprecated brand-icon imports remain in any package or app. Confidence: 0.75
- Deprecated lucide icons are NOT limited to brand/social icons: the icon-map generator scripts must exclude every deprecated icon (not just the brands), and each deprecated icon must be resolved to an inline, customizable SVG in the icon registry rather than resolving to a deprecated/null lucide component ("update lucide to the recent most reliable version... ensure our scripts does not include deprecated icons in our icon-map.ts... every deprecated is resolved to an inline, customizable svg"). Confidence: 0.6
- Prefers a strict engine/content split for the reusable docs package: the package should ship only the docs engine (layout, sidebar, search, gallery, live demo blocks) plus a starter/example page, while the library's own authored documentation (guide pages like Auth, Layout, Theming) lives in the consuming app (apps/docs), not inside the package — a consumer installing the package should not inherit the library's own docs pages as the default payload ("if we have to make the docs reusable... all our documentation is not meant to live there"). The auto-generated component gallery/manifest is the exception: it documents the library's own components, so it belongs in the package and is genuinely useful to consumers. Confidence: 0.8
- Prefers docs organized as hierarchical sidebar sections with per-topic child pages instead of one long page: e.g., auth becomes a sidebar section whose children (signin, signup, mfa, forms, guard, presets, user-button) each get their own extensive page, alongside a separate "ready to use" components section and a later section for pre-built landing pages. Each child page should extensively serve its function. Confidence: 0.75
- State-machine / process-flow diagrams in docs must be rendered as a flowchart or a tree graph (via a graph library like React Flow, installed in the docs package) and must mirror the real state machine: a diagram that invents nodes absent from the actual step enum is "not giving what we want." Confidence: 0.7
- Wants a "ready to use" tier of composite components in the library (dropzone, color-picker, qrcode, marquee, form, roadmap, collaborative canvas) alongside the primitive building blocks — not only low-level components. In the docs they are grouped as a category nested inside the Components section rather than getting their own sidebar section. Confidence: 0.65
- Previewability must extend beyond the component catalog: layouts, forms, and every other shipped artifact should also be previewable with live demos, not just components ("not only components should be previewable... every other thing should be previewable... the layouts, forms etc."). Confidence: 0.7
- Foundations/theming entries (e.g., Icon, Theme) should be excluded from the Components sidebar section: they belong under their own Foundations docs section, separate from the component catalog. Confidence: 0.6
- Interactive demo blocks should couple the live preview with a method/variant switcher and a copyable code block that updates to match whatever is currently previewed: switching the method changes both the preview and the code shown for copy. Confidence: 0.7
- Interactive demo blocks must be responsive: live preview and code side-by-side on large/desktop screens, stacked vertically on smaller screens with the code block underneath; a preview-plus-diagram layout follows the same rule (side-by-side on desktop, vertically stacked on small screens). Confidence: 0.75
- Demo/preview layouts must be responsive: preview + code (or diagram) side-by-side on large screens and stacked vertically on small screens, with the code block underneath. Confidence: 0.65
- Flow diagrams in docs are viewed as "more workload" than they're worth: the user leans toward removing elaborate diagram blocks in favor of simple preview + code demos ("even if the diagram is necessary... the diagram is looking like more workload for me"). Confidence: 0.6
- Landing/marketing site pages should share consistent site chrome: the navbar should appear on every routed page (e.g., the feedback page), not just the home page : the user proactively asks whether a page is missing the navbar ("do you think we should put our navbar in that page also"). Confidence: 0.6
- Dead/unreferenced source files (e.g., a superseded page) should be deleted outright, but only after verification: confirm the file is not imported anywhere and that a live page fully implements its functionality before removing it — the user called an unreferenced landing page "actually useless" and asked to confirm zero references plus full coverage by the live page, then delete. Confidence: 0.7
- Broken or redundant docs content should be removed rather than kept: a live demo that renders blank (the /auth/forms LoginForm demo suspended the whole page) or a sub-section that duplicates another page (auth "Layouts" vs the Ecosystem Layout page, which already covers AuthLayout) is removed, not debugged or cross-linked — the user's default is "if it is not useful there, i think we should remove it," with code/text documentation and the surviving canonical page as the fallback. Redundancy is a reason to delete, not to keep both copies in sync. Confidence: 0.7
- Flex column main content areas must include `min-w-0`: flex items default to `min-width: auto`, so without `min-w-0` on the main chain a child's `overflow-x-auto` table wrapper can't actually constrain wide tables and the whole page overflows horizontally on mobile. The fix is `min-w-0` on the flex main area (not forcing horizontal page scroll), verified in a real 390px mobile viewport (no page overflow, tables scroll inside their own box). Confidence: 0.6
- Semantic status variants in components (success/warning/destructive) should color the text with the semantic token — `text-success` (green), `text-warning` (amber), `text-destructive` — matching the destructive pattern, not `text-foreground`; the tokens were already green/amber and only the variant class wiring was wrong. Confidence: 0.55
- When the user reports a broken subset of pages and says "not sure if they are the only ones though," they expect a complete sweep, not just a fix of the reported ones: enumerate and individually verify every page in the section against the production build, separating working demos from broken ones (e.g., sign-in rendered while sign-up/mfa/guard were silently blank) rather than assuming all demos sharing the same provider/pattern behave alike. Confidence: 0.65
- Live docs demos that mount network-bound auth components against a placeholder client (ArcProvider + a dead `demo.invalid` base URL) silently suspend React's render — blank page with zero children and no console error, unhandledrejection, or window error — and the crash reproduces in the production build, not just dev; a demo's safety cannot be judged from its code pattern alone, and the accepted fallback is code-first documentation (accurate copyable snippet + prose, live render dropped). Confidence: 0.6
- Long numeric input codes (e.g., an 8-digit OTP) should be rendered in grouped, separated segments (3-2-3 with separators) rather than a flat run of digits, because grouped codes are easier to read and verify: the user directed the 8-digit InputOTP variant to the 3-2-3 format, and the layout change is expected to stay in sync across the variant gallery, usage snippet, and a regression test (two separators asserted). Confidence: 0.6
- Docs pages for full-app layout shells (ConsoleLayout, AuthLayout, LandingLayout, Sidebar/Topbar) must be code-first — text + copyable snippets, no live previews — because their fixed/sticky-positioned chrome escapes any CSS container and takes over the docs shell (the /layout page's live demo blocks rendered an arc-id sidebar over the facet docs). When the live previews kept breaking, the user's directive was to go "text-only with code snippets (no live renders at all)" and remove every live demo block from the page, not to keep debugging the previews — the code-first fallback that already applied to network-bound auth demos extends to any demo whose chrome escapes the docs container. Confidence: 0.6
- Content pages should NOT carry an in-page Back button once keyboard navigation exists: the user removed the page-body Back button from the docs content pages as "not really necessary" because the working Alt+←/→ keyboard actions already cover page-to-page movement — redundant in-page nav chrome is removed outright rather than kept (this reverses the earlier "add a Back link at the top of the page body" direction; the button was deleted, not relocated again). The general principle: if a keyboard shortcut or existing mechanism already covers a function, don't duplicate it with visible UI. Confidence: 0.7
- Marquee tracks must not force `whitespace-nowrap` on block children: scrolling card surfaces (e.g., Card-based marquee items) should wrap their inner content naturally instead of clipping, while only plain inline text items need nowrap — applied by wrapping string items in nowrap spans and leaving card nodes free. Confidence: 0.55
- Interactive hover effects like pause-on-hover must follow the standard enter/leave contract: pointer entering the element pauses (stops the scroll/animation) and pointer leaving resumes it — the user clarified the intended semantics as "mousein would pause, while mouseout would resume" when the marquee's hover-pause wasn't working, and expects the behavior to be verified in the running page, not just assumed from the CSS being present. Confidence: 0.55
- Behavior fixes must be verified live in a real browser on BOTH desktop and mobile, not just reasoned about: after a claimed InfiniteScroll fix (removing the forced max-h) the user reported "still the same for the infinite scrol.. both on desktop and mobile", and only live browser-automation verification (driving the running docs page, scrolling the container to the bottom, watching items load 20→30→40→50→60) actually proved the fix — static geometry/CSS reasoning and a green typecheck are insufficient evidence that a behavioral fix works. Confidence: 0.6
- When a whole category of items disappears from navigation surfaces (sidebar, command palette, gallery grid), check whether a category-level exclusion filter is over-broad: an exclusion meant for one set of entries (the extended layout guide entries with their own pages) was applied to the entire "layout" category, wrongly hiding 11 base UI components (Accordion, Breadcrumb, Tabs, Sheet, ...) from the sidebar, search palette, and gallery — the fix narrows the exclusion to the specific slugs that need it. The user's "some components missing" report was confirmed as a real bug across every nav surface, not a search quirk. Confidence: 0.7
- Hover must never open/toggle navigation or panel surfaces — hover is reserved for affordance only: in the layout overlay mode, hovering the mobile brand trigger merely morphs the icon into a "window" affordance, and the sidebar opens ONLY on click (trigger click, or the desktop rail click); it closes on trigger re-click or outside click. The user corrected hover-to-open as wrong ("the overlay is just opening the sidebar on hover... the sidebar only open, when the window is clicked... when a consumer hovers on the logo, the sidebar is not triggered, but the logo turns to the window, and then when clicked, the sidebar appears") — hover reveals the affordance, click performs the action. Confidence: 0.8
- Sidebar navigation must strictly separate section/group headers from leaf items: clicking a section header (e.g., "Ecosystem") only expands/collapses that section and must NOT close the sidebar; only clicking a leaf item (e.g., "CLI" under Ecosystem) navigates and closes the overlay. The user reported the sidebar closing on a section-header click as a bug ("opening ecosystem should not close rail on mobile, but opening or clicking on cli under ecosystem should close it"), and the fix was to verify section/group headers never call the close handler — only leaves do. Confidence: 0.85
- Docs for feature-filled/composite components must convey the FULL concept of what the component can do, with concrete implementation examples per shipped capability — scenario tabs beyond the basic default (DataTable toolbar/export/actions + rows-per-page, Dropzone accept-filter/paste, QRCode logo, NumberInput currency picker, Form validation with a real schema, InfiniteScroll height guidance) — and this depth is explicitly prioritized for the feature-rich composites over the common primitives the user assumes everyone knows: "well detaled enough for consumers to get the whole o full concept of what exists, and examples of how they might be implemnted... especially the full components... not the common most familiar one... the ones we feature filled." Mere coverage (a usage tab existing) is not enough; the content must show consumers how to implement each real scenario, and the code tabs should stay in lockstep with matching live variant preview cells. Confidence: 0.75
- Cascading location selects should label their levels with country-appropriate terminology resolved from the chosen country rather than a generic "state/region": state (NG/US/IN/BR/AU), county (KE/IE), province (ZA/CA/RW), governorate (EG), emirate (AE), prefecture (JP), country (GB), region (GH/TZ/ET/UG) — and the locality level adapts too (LGA for NG, county for US, district for GB/GH). Exposed as resolvers (getRegionLabel/getLocalityLabel) so the same dynamic terms flow through the composed picker and standalone inputs. Confidence: 0.55
- Long option lists in data-heavy selects (774 LGAs, 250+ countries) should be searchable by default — a search box at the top of the dropdown filters the list as the user types, so entries are found by typing rather than scrolling; the pattern applies across every level of a cascading picker, not just one select. Confidence: 0.55
- Library packages (components, layout, auth, any other) must contain ZERO hardcoded brand/vendor-specific values in their default surfaces: hardcoded brand hex colors (navy bg `#0A1A2F`, cyan `#4AD3F5`, gold `#D4AF37`), brand names, and brand-specific footer text (e.g., "ArcevoCirqle Ecosystem") in the AuthLayout default were all flagged as violations and replaced with theme tokens (`bg-primary`/`text-primary-foreground`) plus config-driven fields (`brand.footerText`). The mandate is repo-wide and explicit: "we are not meant to hardcode anything" — every default must be theme-token driven and config-driven so any consumer fully customizes via config or slots. Confidence: 0.8
- Architecture/planning docs for shared client-core packages (SDK/stores/hooks/CLI) should be rewritten as ONE canonical set of the most framework-agnostic conventions possible (headless core + thin UI kit in one monorepo, strict one-way layering page → hook → store → SDK → API, injectable persistence, config-driven UI, CLI that executes real tasks) rather than framework-coupled designs; the user explicitly delegated authoring the arc-id client-core planning doc to the assistant's best judgment ("the perfect most agnostic conventions you see as the best"), including permission to overwrite the prior analysis file outright. Confidence: 0.6
- Theme toggling must be a built-in, config-driven feature of chrome components (Navbar `showThemeToggle`, Topbar/ConsoleLayout `themeToggle`), never hand-wired per app: the user's pre-changeset checklist was to "confirm that our navbars have the theme-toggles in them and across the system, there are no hardcoded theme toggle or states" — so landing/docs/consumer navbars use the built-in prop instead of manually injecting `<ThemeToggle />`, keeping theme UI and theme state centralized in the library. Confidence: 0.7
- The component library should ship animated/micro-interaction variants as first-class component variants (Button: shine sweep, ripple ink-burst, magnetic cursor-gravity; Card: tilt, gradient-border, zoom, flip) — the user asked for "some other components animated variants... and the card could have the flip variant... you know some kinda revealing cards" as part of a release batch. Interactive behaviors must be state-driven, not pure CSS-only: the flip reveals on hover, returns on mouse-out, and toggles on click so touch devices (which have no hover) still work, with a direction choice (horizontal/vertical). Confidence: 0.65
- Reference datasets bundled with components should be sourced from a maintained open dataset via a reproducible generator script + vendored snapshot, not hand-curated or runtime-API: when offered the choice for the countries/states data, the user picked the "generate from open dataset (Recommended)" path (dr5hn countries-states-cities, ODbL) — vendor a snapshot under scripts/data for offline regeneration, keep the generator committed, and preserve any manually-maintained special-case layer (Nigeria's 774 LGAs) on top. Hand-curation was already proven error-prone when a splice silently dropped Nigeria's 37 states. Confidence: 0.55
- Full-page components (FeedbackPage, Footer) deserve a dedicated "Pages" docs section with live preview + usage + customization guidance ("full review and how to customize or reuse it"), separate from the primitive catalog and the ready-to-use composites: the user asked for page components to get their own treatment in the docs ("there should be like a pages... full review and how to customize or reuse it") — new page components land in that section automatically via a manifest category. Confidence: 0.6
- Shared capabilities must live at the shared primitive level, not buried in one consumer's private helper: when the user asked whether the LocationPicker's sticky search was implemented "from the select component... so we know it universal," the fix was to promote it into a first-class `SelectSearch` export on the Select component (any Select consumer can add type-to-filter search by dropping it into SelectContent) and deduplicate the LocationPicker's markup to use it — a feature proven in one composite should be generalized into the underlying shared component so every consumer gets it. Confidence: 0.75
r Ecosystem) navigates and closes the overlay. The user reported the sidebar closing on a section-header click as a bug ("opening ecosystem should not close rail on mobile, but opening or clicking on cli under ecosystem should close it"), and the fix was to verify section/group headers never call the close handler — only leaves do. Confidence: 0.85
