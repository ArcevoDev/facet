# Communication
- Prefers concise, direct go-ahead signals (e.g., "sure - proceed...", or simply "continue" to resume leftover work from a prior session) when satisfied with a proposed plan: trusts the assistant to execute the full plan without micromanaging or requiring per-step re-approval. Avoids verbose confirmation cycles when the direction is already clear; a terse imperative like "run - node gen-snapshot.js ... commit our changes and push to main branch.... then publish our package..." delegates the whole sequence in one line. A bare "continue" is also expected to carry the design decisions already agreed in earlier discussion ("continue... don't forget our signin state machine diagram or flowchart... we discussed..") : those decisions are commitments to be remembered and delivered, not topics to re-open. Re-confirmed when "continue..." alone resumed a tracked priority list (P3 CLI smoke test) from the .agent tracker with no further direction, and again when "continue.." (with only an IDE file context, no restated spec) resumed in-flight crash-recovery work — the bare signal is expected to carry all prior decisions and reconstruction plans forward. Re-confirmed yet again when a bare "continue...." with a clean committed tree resumed the entire remaining verification round (docs-site rebuild check, bundle/dist feature inspection, tracker update, publish handoff) with no further direction. Re-confirmed once more when the user opened a new session by pasting only a session id plus "lets have it then.." — expecting the agreed next step (wiring the generated icon registry into the live consumer) to proceed immediately without re-approval or restating the plan. Confidence: 0.98
- Dislikes AI-sounding marketing copy: explicitly flags em dashes (":"), boilerplate descriptions, and generic "from zero to production" phrasing as "AIish." Prefers natural, human-sounding copy that avoids patterns typical of AI-generated promotional text. Applies to landing pages, READMEs, and any public-facing documentation. Emdashes are banned repo-wide as an explicit style rule ("purge emdash everywhere"): the emdash character (—) must not appear in any source copy or code, with commas, colons, or other punctuation used instead. Explicitly re-affirmed when the user had to manually strip emdashes the assistant had added to docs-page lines and restated the rule: "Emdashes (—) are not allowed in docs pages or any copy." Re-affirmed when the user asked to "confirm there are no emdashes in any unnecessary place in our repo" as part of a landing/docs refresh — a repo-wide sweep is expected, with legitimate markers (e.g., a table-cell "no value" placeholder) kept. Confidence: 0.97
- Wants the assistant to confirm the actual root cause of a reported error before fixing it ("confirm and fix"): diagnose with concrete evidence (files on disk, running processes, resolution paths), apply the fix, and verify the fix end-to-end before reporting done : rather than jumping to a speculative fix or churning config. Re-affirmed when a pasted CI log overturned the documented theory (AGENTS.md said the old publish job died on "repo-fetch errors", but the log showed the real cause was a missing `permissions: contents: write` block producing a 403 push denial) — pasted evidence can and should override stale docs about what broke. Re-affirmed again when a claimed InfiniteScroll fix — reasoned out from static geometry/CSS analysis but never run in a browser — came back as "still the same... both on desktop and mobile": a fix only counts as fixed once verified end-to-end (here, live in the running docs page with browser automation). Re-affirmed when the InfiniteScroll fix, proven live with agent-browser against the running docs page (scroll-to-bottom loads 10 more items each time, 20→30→40→50→60), was the only fix that satisfied the user's repeated "still the same for the infinite scrol.. both on desktop and mobile" report. Confidence: 0.85
- When the user says the assistant has been "debugging the wrong work" and restates the actual problem, pivot immediately to the user's stated symptoms and treat the restatement as authoritative : do not keep iterating on the prior hypothesis (the assistant burned many steps on a stale-build/overlap theory before the user clarified the real issue: topbar content misplaced into the page body and no `<Outlet />` rendering). Confidence: 0.6
- Defects are reported by the visual symptom, not the root cause: the user describes what renders wrong (e.g., "the accordion is displaying all its variants in the grid") and expects the assistant to investigate and find the cause (e.g., an over-eager preview component) : symptom-first reporting, not diagnosis. Confidence: 0.75
- Prefers multi-part questions answered in single concise lines ("write in single lines") rather than long prose sections: each question gets a one-line verdict/bullet, with detail available on request. Confidence: 0.6
- Decision questions are asked with the user's own lean ("should we delete the deploy and leave it as our internal docs... or leave the storybook deploy"), or as an open "do you think we should X" opinion ask ("do you think we should put our navbar in that page also", "do we leave as is or remake like we did github in our registry"): expects a recommendation with rationale, not just an options recap. Re-affirmed when the user asked whether facet component styles needed updating "or you think its fine as-is" and expected a deferral verdict, not an options recap. Confidence: 0.76
- When the user signals doubt about whether they communicated ("i dont know if i am communicating"), confirm understanding by restating the requirement back before implementing ("You're communicating clearly — I get it: ..."), then proceed with the restated spec as the contract. Confidence: 0.55
- When the assistant offers equivalent workflow options and asks the user to choose, the user defers the decision back to the assistant ("whichever is best...") and expects it to pick the option that leaves the least manual work for them — here, running `changeset version`, verifying the build against the bumped versions, and committing so the user's release becomes a single `pnpm changeset publish` — then execute that choice fully without re-asking. Re-affirmed when the user delegated landing routing decisions the same way ("we could even adjust or reshape the routings... whichever is best..."), and again when asked whether to publish the CLI now or start the next feature first ("whichever is best..." → assistant picked the publish-that-unblocks step and executed). Re-affirmed once more when, after the assistant laid out crash-recovery options and recommended one (restore from git + re-extend LightIcon), the user approved with "lets go with the most recommended options..." — taking the top recommendation as the instruction and expecting full execution without re-opening the tradeoffs. Confidence: 0.75
- Bug reports arrive with reproduction evidence pasted verbatim: the exact command the user ran and its raw output (e.g., `node scripts/gen-icon-map.mjs` → "Wrote icon-map.ts (1763 icons)"), followed by the user's own tentative hypothesis about the cause ("maybe it is also deprecated...") — the pasted command/output is authoritative context to work from, while the hypothesis is a lead to confirm or refute with on-disk evidence, not a directive or an established fact. The paste can also arrive entirely wordless — just a raw `pnpm facet pkg` / `facet up` transcript from a consumer repo — and the expectation is still to spot the anomaly in it (table showing `installed: -` for every package while `latest` is newer, yet the tool claims "All up to date"), root-cause it, and fix it without needing the user to spell out the problem. Re-affirmed when the user pasted a `pnpm facet pkg` transcript from a consumer repo (arc-id) with only "i think you have to see this": every `latest` cell read `n/a` while the tool claimed "All installed facet packages are up to date" — the anomaly to spot was the `n/a` latest column. Root-causing it required running the actual published CLI binary directly inside the consumer repo (registry fetch verified against the live registry, published dist confirmed to carry the robust resolver, direct run detecting all 4 updates) before concluding the `n/a` was a stale cached pnpm shim rather than a CLI bug — so the evidence-based verdict can legitimately be "environmental, no code fix needed," and a rigorously-justified no-fix conclusion is as valid an outcome as shipping a patch. Re-affirmed when the user pasted, wordless, the vitest run of the logged emails command showing 5 failed render tests (tree-form primitives dropping variadic children + one wrong brand assertion): the expected response was to root-cause each failure from the assertion diffs, fix the primitives and the test, and hand back the same command to re-run — no spelling-out of the problem needed. Re-affirmed again when the user pasted, wordless, a failed full-workspace `pnpm -r build` (emails package: tsup multi-entry self-import + TS6133 type-only import) and the assistant root-caused both errors from the log and committed the fixes. Re-affirmed yet again in the same episode's follow-up: the user re-ran the gate after the fixes and pasted the next failure wordlessly (TS6196 on the now-unused EmailBrand type import), and the assistant root-caused it from the log, fixed, and committed — each wordless build-log paste is a complete work order needing no explanation. Re-affirmed once more when the same gate's third failure (TS6133 on an unused `name` param) was pasted wordlessly and handled identically — root-cause from the log, fix, commit, hand back the re-run command — confirming the wordless-paste loop repeats until the whole gate is green. Re-affirmed again when the post-AnimatedButton `pnpm -r build` failure (TS6133: unused `AnimatedButtonRenderProps` import in billing-page.tsx) was pasted wordlessly — root-caused from the log, dead import removed, sibling-file usage verified, committed, and the re-run command handed back. Confidence: 0.92
- When a build gate fails immediately after the assistant's own changes, a terse "did you break sth..." expects an honest accountability verdict grounded in the pasted log: attribute the failure precisely to the assistant's changes (here the components dts TS18048s traced to the assistant's Footer refactor), confirm the rest of the workspace built green, fix via a comprehensive package audit rather than a one-file patch, commit, and hand back the exact re-run command — not defensiveness, not another blind fix loop. Confidence: 0.6
- Before resuming or finishing in-flight work, wants half-finished/uncommitted changesets cross-checked against the project's documented rules (e.g., AGENTS.md CI contract) and the declared dependency surface (deps declared but never imported, keyframes/theme vars referenced but not defined) — discrepancies are surfaced as deliberate-decision points rather than silently resolved. Confidence: 0.5
- A reversal clause appended to a proceed signal ("continue.. that was a mistake..") right after a change was executed is an undo directive for that change, not a throwaway aside: when the user says a just-performed change (here: removing the overlay sidebar mode) "was a mistake," they expect the assistant to identify which change is being reversed and revert/reconsider it before continuing — treating it as a pure continue signal and pressing on with the removal was a miss. Confidence: 0.5
- Terse planning prompts like "whats next..." or "what work is yet to be done here in facet..." delegate next-step determination entirely to the assistant: the expected response is to consult the local tracker plus git/repo state (scoped to whichever repo the question names, even when the user attaches only an IDE file context), summarize what's outstanding (separating done/committed from open, and separating work belonging to sibling repos like arc-id), and propose a prioritized pick with rationale — not to bounce the question back to the user. Re-affirmed when the user asked "what work is yet to be done here in facet..." with only `packages\cli\package.json` as IDE context and expected a repo-state-derived status report (blocking publish, stray artifact, stale tracker, deferred items) rather than a clarifying question. Confidence: 0.6
- When reporting what's open, the accounting must be EXHAUSTIVE: a single-item answer to "what's still open" gets challenged ("are you sure item 13 is the only thing this repo needs to checkout..."), so the assistant must enumerate every outstanding item — the blocking steps for the current batch (e.g., unwritten changeset, uncommitted work) plus each deferred backlog item (CI publish, tree-shakeable icons, chunk-size warnings) — not just the headline item that was already known. Confidence: 0.6
- Idea questions are posed as a first-person hunch to react to ("i am thinking it could be dynamic, whereby the selected country... would determine the state to be displayed... what'd you think... you can just use the world map for what exiss"): the assistant is expected to validate/refine the idea against the actual code and implement it with judgment (externalizing the dataset, full real-world data, dynamic country tags), not to stall on clarifying questions. Re-affirmed with "i think we should have the dynamic full state list across other countries too, at least.. what'd think.." — a follow-up directing uniform completeness across all covered countries, again as a lean to execute. Re-affirmed again with "i think if it can - its worth the upgrad to save stress... what'd you think..." (whether the CLI's icon scanner could capture object-literal icons in a consumer repo): the expected response was to go read the consumer's actual files, confirm the capability gap against the real code, and implement the upgrade — not to debate the idea or stall on clarifying questions. Confidence: 0.7
- Requests phrased with an informal, self-correcting tone ("let us ensure that the every components page is well detaled... especially the full components... not the common most familiar one... the ones we feature filled... lol...") still carry a concrete, priority-ordered deliverable: audit coverage, then enrich the feature-filled composite components' pages with full-concept docs, prioritizing the complex composites over the commonly-known primitives. Confidence: 0.6
- When the assistant declares a generated deliverable (e.g., the docs draft from `facet docs scan`) "perfect and well to work with," the user expects it verified by actually rendering it in a real browser (via a localhost dev server or browser automation/chrome extension), not just by inspecting the generated files on disk — "just fully review" means a complete end-to-end review of the rendered output, and because the user is often busy elsewhere (e.g., assembling the arc-id repo), the assistant should run this verification autonomously and report back rather than pausing to ask for confirmation first. Confidence: 0.6
- When asking whether a codebase is ready to rest and be consumed broadly, expects an honest, two-sided readiness verdict — what's solid and publish-ready alongside the real caveats (deferred backlog, the one smoke test still pending, the sequenced next big thing) — with a clear "rest it, and do exactly this one checkpoint before trusting it everywhere" recommendation, rather than an unqualified "yes it's done." Confidence: 0.7
- When the user asks whether the deferred/perf backlog should be tackled now ("what do you say about this... should we get this done with now"), they expect a grounded, per-item triage rather than a blanket "do all now" or "defer all": measure each item's real scope from the code (e.g., icon-map 106KB/1763 icons; docs-site main chunk 294KB vs the >500kB lazy chunk), then classify each as worth-doing-now (compounding value that also fixes downstream symptoms — tree-shakeable icons shrink consumer bundles AND resolve the chunk warning), wrong-time (work belonging to a repo currently mid-change — arc-id CLI while the user is actively doing arc-id UI updates), or a symptom of another item (the chunk warning, fixed at the source by the icons work) — and end with an explicit do/skip verdict per item plus a recommended next action. Confidence: 0.6
- Consumer-side verification of freshly published packages is handled by the user's own real-world usage: they install the latest versions in their consumer repos (arc-id) and actively work against them, reporting breaks as they surface ("we already installed the latest version in arc-id... would let you know if anything breaks") — the assistant should treat that active consumption as the consumer smoke test and proceed with further work rather than insisting on running a formal one itself. Re-affirmed explicitly after a publish when the user said "we are currently testing our updated pkgs in [the] consuming file, you could wait for reviews" — the assistant's next-todo planning should treat consumer feedback as the gate for further package work and hold off on it until reviews come in. Confidence: 0.62
- Delegation phrased as "work the X from here... confirm everything is well replaced... just ensure the files and setup are perfect and well defined" is a verification-first work order: execute the edits, then prove the result is complete and correct (exhaustive sweeps, import-surface validation against the published artifact, definitive clean report) before handing back — and the assistant's final summary for such a task must itself be thorough and complete, mirroring the "well defined" standard. Confidence: 0.7

- Copy should be poetic and human — the user self-identifies as "a poetic dev": landing CTAs and headings favor evocative phrasing ("Begin your journey", "Welcome back", "A key to every door") over generic "Get started"/"Sign in", and hero typewriter phrases should be short (5-7 words), truthful/canonical about what the product does, and read as a continuation of the static headline ("One identity, [cycling phrase]") rather than standalone sentences. Confidence: 0.7
- Answers architecture/decision questions inline in the planning docs rather than in chat: when the assistant leaves open questions at the end of an analysis file (e.g., client-core-architecture-analysis.md), the user's answers are edited into the doc itself ("we already answered the questions you asked in the client-core-arch.... planning docs file") — read the doc for the decisions before re-asking. Confidence: 0.6
- Wants real user flows exercised from the running UI ("i think its high time we started testing from our ui"): social OAuth, magic link, and post-auth routing should be validated by clicking through the live auth pages in the browser, not only via programmatic E2E scripts. Confidence: 0.55
- Explicitly wants a release/bump to carry substantial new work, and proactively pads the batch with more features ("before commiting and bumping version... i thik we should utilize this chance better - lol - could we add a few more pages in the components pkg... so we have a wonderful changeset") — new page components, email templates, and a whole new package were proposed to make the changeset meaningful rather than shipping a thin bump. Re-affirmed when the user asked for a full pre-changeset ideation sweep before any release or new package: "before we make any changeset or consider any other pkgs... what are the key refinements that could be made to components, auth or layout... could you suggest 2-3 more best layout options to consider, any more refinement beyond our present scope to auth, any more animation about 12-25 more we could build, or any thing you can ever suggest, new working components, or any bugs caught in sdk" — the expected response is a broad menu of refinement proposals across categories (layout options, auth refinements, a large 12-25 item animation batch, new components, SDK bug sweeps) so the upcoming changeset is meaningful rather than thin. Confidence: 0.7
- Open-ended feature ideation is framed as a first-person hunch with playful self-aware filler ("i was thinking... maybe... or i have another beautiful idea.. could we build something like react-email... if you think this is feasible... lol..."): the user floats half-formed ideas (email templates, a react-email replacement) and expects the assistant to treat them as real proposals worth exploring and planning, even offering alternative paths — not dismiss them for informality. Confidence: 0.55
- New capabilities must let consumers fully customize branding without forking internals: the email package's branding works at two levels — a token object (primary/background/surface/text/muted/fontFamily/radius/brandName) theming every primitive, plus per-primitive `style` overrides — and the consumer keeps its own tokens file feeding the brand option (arc-id's `tokens.ts` maps into `renderEmail`'s brand), so a consumer's full look comes through without touching package internals. Confidence: 0.55
- When the user follows the assistant's handoff instructions for a release and hits an error mid-flow, they check in with "was this expected" before proceeding further: they want a definitive verdict distinguishing expected sequencing artifacts (e.g., `pnpm changeset status` erroring with "no changesets found" after `changeset version` because the version-bump commit hasn't landed) from real problems — with the root cause explained, the fix executed (commit the bump), and the remaining exact commands handed back — so they can proceed to publish with confidence rather than guessing whether the error is a blocker. Confidence: 0.6
- After a fix or refactor lands, a question like "will this affect the X in anyway" asks for a precise impact trace, not generic reassurance: trace where that feature's behavior actually lives (e.g., for email branding: the EmailBrand interface re-export, renderEmail's brand option, the style-block generator, the consumer's brand-token mapping, and the scaffold's brand.ts), prove the diff doesn't touch it, and give a definitive "same behavior, just cleaner" verdict. Confidence: 0.55
- Batch-scope confirmation questions ("just for confirmation.. did the components all-round animation touch our pkgs too, like the email, auth pkg, and other concerned ones...") expect a definitive, evidence-based per-package accounting, not a recall-based guess: derive the answer from actual evidence (git show --stat of the batch's commits + the changeset files) and explicitly separate (a) directly-modified packages, (b) dependents that only get version-bumped via changesets, and (c) packages explicitly untouched, with the reason (e.g., emails are framework-agnostic and don't consume React animated buttons) — ending with the complete pending release set. A package wrongly claimed as touched (or missed) is precisely the imprecision such a question guards against, and any release-coverage gap found during the audit is closed before the answer is given. Confidence: 0.6
