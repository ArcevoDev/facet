---
"@arcevo/facet-layout": minor
---

Add VS Code-style resizable sidebar to rail mode: right-edge drag handle resizes the expanded sidebar (176-400px, persisted), dragging below the minimum collapses to the icon rail, and clicking a rail icon expands it again. `ConsoleLayout` accepts a `topbar` prop for injecting content (links, toggles) into the top bar.
