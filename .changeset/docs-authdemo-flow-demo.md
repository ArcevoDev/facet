---
"@arcevo/facet-docs": minor
---

- Adds a live-linked `SignInFlowDemo` component and `signInFlowDemo` docs block: an interactive `SignInFlowDiagram` drives a real controlled `<SignIn>`; clicking a node animates the trace and advances the live sign-in surface (and vice-versa).
- Adds the `AuthDemo` component and `authDemo` docs block: a configurable live `<SignIn>` with method/OAuth toggles that generates the exact `config` code.
- Adds the `keyboardShortcuts` docs block (Kbd-chip shortcuts table).
- All three are exported from the package barrel and documented in the docs package README block table.
